experiment:
  name: svr_position_salary
  task: regression
  random_state: 42

data:
  path: data/raw/regression/position_salaries.csv
  target: Salary
  features: [Level]
  test_size: 0.2

model:
  type: svr
  kernel: rbf
  C: 10000.0
  epsilon: 1000.0
  gamma: 1.0

outputs:
  dir: exports
